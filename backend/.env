# Backend Environment Variables - TEST MODE

# Server
PORT=5000
NODE_ENV=development

# Supabase (using your existing credentials)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# JWT Secret
JWT_SECRET=test-secret-key-for-development

# MTN Mobile Money API (Benin) - SANDBOX MODE
# Note: These are placeholder values for testing
# You need to replace them with real credentials from https://momodeveloper.mtn.com/
MTN_MOMO_API_KEY=test-api-key
MTN_MOMO_API_USER=test-uuid-here
MTN_MOMO_SUBSCRIPTION_KEY=test-subscription-key
MTN_MOMO_ENVIRONMENT=sandbox
MTN_MOMO_CALLBACK_URL=http://localhost:5000/api/payments/momo/callback
